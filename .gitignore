# 環境変数（キー名だけを .env.example に書く。Marketplace 公開用トークンもファイルに書かない）
.env
.env.*
!.env.example

# Node
node_modules/

# ビルド成果物・テスト用 VS Code・パッケージ
dist/
out/
.vscode-test/
*.vsix
.cache/
logs/

# OS・エディタ
.DS_Store
Thumbs.db
~$*
*.tmp
*.log
*.stackdump

# 手元だけで使う文書と設定（docs/、CLAUDE.md、.claude/、.automation/、spikes/）は、
# 非公開の foreman-docs リポジトリで管理する。公開側では .git/info/exclude で無視し、
# 追跡されていないことを npm run verify:package で検査する
